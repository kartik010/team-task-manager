# Production environment reference (Railway)
# These are NOT loaded from a file in production — set them on Railway.

DATABASE_URL=${{Postgres.DATABASE_URL}}
NODE_ENV=production
JWT_SECRET=<generate-with-openssl-rand-hex-32>
# PORT is set automatically by Railway
# CLIENT_URL is optional — app auto-detects RAILWAY_PUBLIC_DOMAIN
