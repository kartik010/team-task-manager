{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && cd server && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd server && NODE_ENV=production npx prisma migrate deploy && NODE_ENV=production npx tsx prisma/seed.ts && NODE_ENV=production node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
