# Railway config for the WEB service (GitHub repo deploy)
# IMPORTANT: Your PostgreSQL service name on the canvas must be exactly "Postgres-0G3F"
# (click the Postgres box → Settings → rename if needed — case-sensitive)

[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build && cd server && npx prisma generate"

[deploy]
startCommand = "bash scripts/start-production.sh"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Config-as-code: inject DB connection from the Postgres service (no dashboard reference needed)
[env]
NODE_ENV = "production"
DATABASE_URL = "${{Postgres-0G3F.DATABASE_URL}}"
DATABASE_PRIVATE_URL = "${{Postgres-0G3F.DATABASE_PRIVATE_URL}}"
PGHOST = "${{Postgres-0G3F.PGHOST}}"
PGPORT = "${{Postgres-0G3F.PGPORT}}"
PGUSER = "${{Postgres-0G3F.PGUSER}}"
PGPASSWORD = "${{Postgres-0G3F.PGPASSWORD}}"
PGDATABASE = "${{Postgres-0G3F.PGDATABASE}}"
