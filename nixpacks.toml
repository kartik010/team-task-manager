[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["NODE_ENV=development npm install"]

[phases.build]
cmds = ["npm run build", "cd server && npx prisma generate"]

[start]
cmd = "bash scripts/start-production.sh"
